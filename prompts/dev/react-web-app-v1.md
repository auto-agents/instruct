**Prompt for React Hello World App**

Create a minimal React web application with the following requirements:
1. A single-page landing page.
2. The page should display the text 'Hello, World!' prominently in the center of the screen.
3. Use modern React best practices (e.g., functional components, hooks if needed).
4. Include basic styling (CSS or CSS-in-JS) to ensure readability and aesthetics.

Provide:
- Complete code for `src/App.js`.
- Setup instructions using `create-react-app`.

Optional enhancements:
- Add routing or state management if desired.
