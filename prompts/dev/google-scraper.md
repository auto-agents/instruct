write a google scraper in python that takes a search query as input from command line arguments, and returns the top 10 search results with their titles and URLs. write the script in `/mnt/e/tmp/gscrapper/gscrapper.py`. The script should use the `requests` library to fetch the search results page and `BeautifulSoup` to parse the HTML content. The output should be printed in a numbered list format, with each title and URL on a separate line. Additionally, save the output in a file named `google_search_results.txt`, in the same directory of the script.
Do not uses a mock implementation or an authorized API, the script should directly scrape the Google search results page. Make sure to handle potential issues such as HTTP errors and changes in Google's HTML structure.
Do no expose a plan, writes directly the files. Don't add any explanations or comments.
